# The Firm — frontend env

# The CasePilot Flask backend (your teammate's repo: dheer309/lawhive-hack).
# Defaults to the Flask dev port if unset. The UI calls this directly (CORS is
# enabled on the backend). If unreachable, the UI falls back to the demo replay.
NEXT_PUBLIC_BACKEND_URL=http://localhost:5001

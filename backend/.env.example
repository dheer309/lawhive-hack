# Copy to backend/.env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-...      # required for real pipeline output
OPENAI_API_KEY=sk-...             # optional — enables voice transcription (Whisper)
